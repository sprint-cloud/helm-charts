apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sprinthub.fullname" . }}-test-connection"
  labels:
    {{- include "sprinthub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sprinthub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
