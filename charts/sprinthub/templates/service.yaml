apiVersion: v1
kind: Service
metadata:
  name: {{ include "sprinthub.fullname" . }}
  labels:
    {{- include "sprinthub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sprinthub.selectorLabels" . | nindent 4 }}
