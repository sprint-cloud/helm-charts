apiVersion: v1
kind: Service
metadata:
  name: {{ include "pipelines-as-code.fullname" . }}
  labels:
    {{- include "pipelines-as-code.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pipelines-as-code.selectorLabels" . | nindent 4 }}
