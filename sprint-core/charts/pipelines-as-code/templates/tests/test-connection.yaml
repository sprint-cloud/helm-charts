apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pipelines-as-code.fullname" . }}-test-connection"
  labels:
    {{- include "pipelines-as-code.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pipelines-as-code.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
