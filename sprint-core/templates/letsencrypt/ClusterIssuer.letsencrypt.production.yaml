{{ if .Values.letsencrypt.staging.enable }}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-staging
spec:
  acme:
    server: {{ .Values.letsencrypt.staging.server }}
    email: {{ .Values.letsencrypt.email }}
    privateKeySecretRef:
      name: letsencrypt-staging
    solvers:
      - dns01:
          cloudflare:
            email: {{ .Values.letsencrypt.email }}
            apiTokenSecretRef:
              name: {{ .Values.letsencrypt.staging.tokensecret }}
              key: cloudflare-token
        selector:
          dnsZones:
            {{- range .Values.letsencrypt.dnsZones }}
            - {{ . | quote }}
            {{- end }}
{{ end }}